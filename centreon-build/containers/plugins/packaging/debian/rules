#!/usr/bin/make -f

export DH_VERBOSE=1

%:
	dh $@

override_dh_auto_clean:

override_dh_auto_install:
	mkdir -p debian/@PROJECT_NAME@@PLUGIN_PATH@
	install -m 775 *.pl debian/@PROJECT_NAME@@PLUGIN_PATH@
