#!/usr/bin/expect -f

### rpm-sign.exp -- Sign RPMs by sending the passphrase.

spawn rpmsign --addsign --key-id=8A7652BC {*}$argv
expect -exact "Enter pass phrase: "
send -- "Secret passphrase\r"
expect eof

## end of rpm-sign.exp