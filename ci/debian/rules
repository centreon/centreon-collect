#!/usr/bin/make -f

%:
	dh $@  

override_dh_auto_configure:
	bash cmake.sh -r

override_dh_auto_build:
	cd build && \
	make

override_dh_auto_install:
	cd build && \
	make install DESTDIR=../debian/tmp-centreon-collect
