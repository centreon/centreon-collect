name: "centreon-engine-bench-debuginfo"
arch: "${ARCH}"
platform: "linux"
version_schema: "none"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
section: "default"
priority: "optional"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Debuginfo package for centreon-engine-bench.
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://centreon.com"
license: "Apache-2.0"

contents:
  - src: "../bin/centengine.debug"
    dst: "/usr/lib/debug/usr/sbin/centengine.debug"
    file_info:
      mode: 0644

  - src: "../bin/centenginestats.debug"
    dst: "/usr/lib/debug/usr/sbin/centenginestats.debug"
    file_info:
      mode: 0644

  - src: "../lib/externalcmd.so.debug"
    dst: "/usr/lib/debug/usr/lib64/centreon-engine/externalcmd.so.debug"
    file_info:
      mode: 0644

overrides:
  rpm:
    depends:
      - centreon-engine = ${VERSION}-${RELEASE}${DIST}
    conflicts:
      - centreon-engine-daemon-debuginfo
      - centreon-engine-extcommands-debuginfo
    replaces:
      - centreon-engine-daemon-debuginfo
      - centreon-engine-extcommands-debuginfo
    provides:
      - centreon-engine-daemon-debuginfo
      - centreon-engine-extcommands-debuginfo
  deb:
    depends:
      - centreon-engine (= ${VERSION}-${RELEASE}${DIST})
    conflicts:
      - centreon-engine-dbgsym
      - centreon-engine-daemon-dbgsym
      - centreon-engine-extcommands-dbgsym
    replaces:
      - centreon-engine-dbgsym
      - centreon-engine-daemon-dbgsym
      - centreon-engine-extcommands-dbgsym
    provides:
      - centreon-engine-dbgsym
      - centreon-engine-daemon-dbgsym
      - centreon-engine-extcommands-dbgsym

rpm:
  summary: Debuginfo package for centreon-engine-bench.
  compression: zstd
  signature:
    key_file: ${RPM_SIGNING_KEY_FILE}
    key_id: ${RPM_SIGNING_KEY_ID}
