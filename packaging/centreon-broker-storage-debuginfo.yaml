name: "centreon-broker-storage-debuginfo"
arch: "${ARCH}"
platform: "linux"
version_schema: "none"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
section: "default"
priority: "optional"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Debuginfo package for centreon-broker-storage.
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://centreon.com"
license: "Apache-2.0"

contents:
  - src: "../lib/{20-storage,20-unified_sql,70-rrd}.so.debug"
    dst: "/usr/lib/debug/usr/share/centreon/lib/centreon-broker/"
    file_info:
      mode: 0644

overrides:
  rpm:
    depends:
      - centreon-broker-storage = ${VERSION}-${RELEASE}${DIST}
  deb:
    depends:
      - centreon-broker-storage (= ${VERSION}-${RELEASE}${DIST})

rpm:
  summary: Debuginfo package for centreon-broker-storage.
  compression: zstd
  signature:
    key_file: ${RPM_SIGNING_KEY_FILE}
    key_id: ${RPM_SIGNING_KEY_ID}
