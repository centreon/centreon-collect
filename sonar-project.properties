# project
sonar.projectKey=centreon-collect
sonar.projectName=Centreon Collect

sonar.sources=bbdo\,
              broker\,
              clib\,
              cmake\,
              connectors\,
              engine\,
              grpc\,
              cmake.sh\,
              CMakeLists.txt\,
              conanfile.txt

sonar.tests=tests

sonar.sourceEncoding=UTF-8
sonar.tsql.file.suffixes=sql,tsql
sonar.plsql.file.suffixes=pks,pkb

#sonar.c.file.suffixes=-
#sonar.cpp.file.suffixes=-
#sonar.objc.file.suffixes=-

sonar.cfamily.compile-commands=build/compile_commands.json
# sonar.cxx.xunit.reportPath=ut.xml

# sonar.cxx.clangtidy.reportPath=clang-tidy-report
# Commented as sonarqube does not support natively clang-tidy and requires a conversion e.g
## $ compiledb make --dry-run build-only
##      clang-tidy -checks='*' -header-filter="^include" src/*.cc -p . > build/clang-tidy-report.txt
##      ./convert-compile-commands.py build/compile_commands.json bw-output
